<UserControl x:Class="Traderdata.Client.TerminalWEB.Dialogs.CommandBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100" d:DesignWidth="200">
    
<Grid x:Name="LayoutRoot" >
        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Bottom" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,0,91,0" VerticalAlignment="Bottom" />
        <TextBox HorizontalAlignment="Stretch" Margin="10,10,10,30" Name="txtNovoComentario"  VerticalAlignment="Stretch" AcceptsReturn="True" TabIndex="0" KeyDown="txtNovoComentario_KeyDown_1" FontSize="30" FontWeight="Bold"/>

    </Grid>
</UserControl>
